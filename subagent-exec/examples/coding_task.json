{
  "schema_version": "1.0",

  "task_id": "CODE-001",

  "objective": "为 add 函数增加单元测试",

  "prompt": "请为 src/math.ts 中的 add 函数创建 pytest 单元测试。测试文件放在 tests/test_math.py。不要修改 src/math.ts。完成后运行 pytest 验证。",

  "cwd": ".",

  "allowed_paths": [
    "tests/**"
  ],

  "constraints": [
    "不要修改 src/math.ts",
    "只新增 tests/test_math.py"
  ],

  "acceptance_criteria": [
    "tests/test_math.py 文件已创建",
    "覆盖至少 3 个测试用例（正数、负数、零）",
    "pytest 通过"
  ],

  "verification": {
    "commands": ["python -m pytest tests/test_math.py -v"],
    "timeout_ms": 60000
  },

  "timeout_ms": 300000,

  "metadata": {
    "task_type": "implement"
  }
}
